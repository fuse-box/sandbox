# monorepo-simple
Monorepo simple


### Run project sample
* `npm install`
* `npm start`

### Build unbundled esNext of packages
* `npm build`

### Publish
* `npm pub-test` <-- test only will not publish
* `npm pub-now`

